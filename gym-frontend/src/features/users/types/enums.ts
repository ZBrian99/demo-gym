export enum Role {
  ADMIN = 'ADMIN',
  STAFF = 'STAFF',
  USER = 'USER'
}

export enum Currency {
  ARS = 'ARS',
  USD = 'USD'
}

export enum Modality {
  FREE = 'FREE',
  THREE = 'THREE',
  TWO = 'TWO'
}

export enum AccessStatus {
  GRANTED = 'GRANTED',
  DENIED = 'DENIED'
} 